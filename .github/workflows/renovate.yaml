---
name: Renovate
"on":
  schedule:
    # Run weekly.
    - cron: "3 17 * * 1"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - .github/workflows/renovate.yaml
      - renovate.json5

jobs:

  renovate:
    uses: kdeldycke/workflows/.github/workflows/renovate.yaml@v5.6.1
